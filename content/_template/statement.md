One paragraph on why this comes up in a real interview. Concrete, not aspirational.

Implement `solve(...)`.

- State the contract as bullets. Every bullet should map to at least one test.
- Say what happens on empty input, and on the degenerate case.
- Name the exception type if the function is supposed to raise.

```python
solve(example)
# expected
```

### What the interviewer is checking

The one or two things a strong candidate does that a weak one does not, plus the
follow-up question you should expect.
